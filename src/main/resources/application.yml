spring:
  application:
    name: personal_bot

logging:
  level:
    root: info
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %5p %class{0}:: %m%n"
  file:
    name: logs/bot_log.log
    max-size: 10MB
    max-history: 5